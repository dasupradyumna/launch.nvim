----------------------------------------- UTILITY FUNCTIONS ----------------------------------------

local notify = require 'launch-nvim.utils.notify'
local validate = require 'launch-nvim.utils.validate'

local utils = { notify = notify, validate = validate }

return utils
